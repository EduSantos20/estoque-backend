package com.loja.estoque.repository;

import com.loja.estoque.model.RelatorioDetalhe;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RelatorioDetalheRepository extends JpaRepository<RelatorioDetalhe, Long> {
}
